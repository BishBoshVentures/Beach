[build]
builder = "nixpacks"
buildCommand = "npx -y pnpm@10.32.0 install --frozen-lockfile && npx -y pnpm@10.32.0 --filter @beach-events/app build"

[deploy]
startCommand = "npx -y pnpm@10.32.0 --filter @beach-events/app start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
